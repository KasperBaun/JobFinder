# Epico Tech (favorite-company career site)

**Prior:** User-declared preferred employer (R-091). Small Danish IT consultancy.

**Findings:**

- `epico.tech` serves a 114-byte JS redirect stub to `/lander` (412 bytes) — a fully client-rendered SPA with no ATS signature in raw HTML (probed 2026-06-12).
- No public feed or API surfaced; company is small enough that openings are sporadic.

**Verdict:** not wired — covered indirectly via the Jobindex feeds, The Hub (DK startup board, already enabled), and the preferred-company boost ("Epico" matches in the skillset). Revisit if they move to a standard ATS (TeamTailor is common for DK consultancies this size).
